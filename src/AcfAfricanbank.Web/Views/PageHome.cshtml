@inherits UmbracoViewPage<ContentModels.PageHome>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@using Umbraco.Cms.Core.Models;
@using Umbraco.Cms.Core.Models.Blocks;
@using Umbraco.Cms.Web.Common.Mvc;
@inject Umbraco.Cms.Core.Models.PublishedContent.IPublishedValueFallback PublishedValueFallback
@inject Umbraco.Cms.Core.Routing.IPublishedUrlProvider PublishedUrlProvider
@inject Umbraco.Cms.Core.Models.PublishedContent.IVariationContextAccessor VariationContextAccessor
@{
    Layout = "~/Views/MasterNew.cshtml";
    ContentModels.PageHome home = Model as ContentModels.PageHome;
    var homePageElements = (HomePageElements)home.Children(VariationContextAccessor).Where(o => o is HomePageElements).FirstOrDefault();
    var heroHeader = (HeroHeaderHome)homePageElements.Children(VariationContextAccessor).Where(o => o is HeroHeaderHome).FirstOrDefault();
    var heroShoulder = (HeroShoulderHome)homePageElements.Children(VariationContextAccessor).Where(o => o is HeroShoulderHome).FirstOrDefault();
    var heroBody = (HeroBody)homePageElements.Children(VariationContextAccessor).Where(x => x is HeroBody).FirstOrDefault();
    //var heroBodyGetLoans = (StructureBodyTextWithImageAndLink)heroBody.Children.Where(y => y is StructureBodyTextWithImageAndLink).FirstOrDefault();
    var heroBodySections = heroBody?.Children(VariationContextAccessor)?.Where(y => y is StructureBodyTextWithImageAndLink).ToList();
    var heroFeetNew = homePageElements?.Children(VariationContextAccessor).Where(c => c is HeroFeet).FirstOrDefault();
    var testimonialsNew = (Testimonials)heroFeetNew?.Children(VariationContextAccessor).Where(c => c is Testimonials).FirstOrDefault();
    var heroBodyBlog = (HeroBody)homePageElements.Children(VariationContextAccessor).Where(x => x is HeroBody).FirstOrDefault();
    var heroBodyBlogLink = heroBodyBlog.Children(VariationContextAccessor).Where(x => x.ContentType.Alias.ToLower() == "link").FirstOrDefault();
    var bottomNavigation = (BottomNavigation)homePageElements.Children(VariationContextAccessor).Where(c => c is BottomNavigation).FirstOrDefault();
    var htmlStringUtilities = new HtmlStringUtilities();
}

<!--Optional code starts here -->
<div class="live-chat animate__animated animate__slideInRight ">
    <i class="fa fa-comments-o fa-3x" aria-hidden="true"></i>
</div>
<!-------Header Section Starts------->
<div class="header-section">
    <div class="header-content container text-center ">
        <!-- HERO HEADER -->
        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
                @{
                    if (heroHeader != null)
                    {
                        int counter = 0;
                        foreach (var items in heroHeader.Items)
                        {
                            <li data-target="#carouselExampleIndicators" data-slide-to="@counter" class="@((counter == 0) ? "active" : "")"></li>
                            counter++;
                        }
                    }
                }
            </ol>
            <div class="carousel-inner">

                @{
                    if (heroHeader != null)
                    {
                        int myCounters = 0;
                        foreach (var heroItem in heroHeader.Items)
                        {
                            var heroContent = heroItem.Content;
                            var heroImageUrl = ((ContentModels.Image)((IPublishedContent)heroContent.Value(PublishedValueFallback, "heroImage"))).Url(PublishedUrlProvider);
                            Umbraco.Cms.Core.Models.Link redirect_links = null;
                            foreach (var link in (IEnumerable<Umbraco.Cms.Core.Models.Link>)heroContent.Value(PublishedValueFallback, "heroRedirectAction"))
                            {
                                redirect_links = link;
                                break;
                            }
                            <div class="carousel-item @((myCounters == 0) ? "active" : "")">
                                <div class="slider-gradient">
                                    <img class="d-block w-100" src="@heroImageUrl" alt="First slide" width="100%" height="100%">
                                </div>
                                <div class="carousel-caption">
                                    <h1 class="ab-jumbo-title"> @heroContent.Value(PublishedValueFallback, "heroTitle")</h1>
                                    <h2 class="ab-jumbo-discription">@heroContent.Value(PublishedValueFallback, "heroProductDescription")<span></span></h2>
                                    <div class="primary-btn">
                                        @{
                                            if (redirect_links != null)
                                            {
                                                var buttonText = heroContent.Value(PublishedValueFallback, "heroButtonLabel");
                                                <a href="@redirect_links.Url"><button> @buttonText</button></a>
                                            }
                                        }
                                        @*<a href=""><button>@heroContent.Value(PublishedValueFallback, "heroButtonLabel")</button></a>*@
                                    </div>
                                </div>
                            </div>
                            myCounters++;
                        }
                    }
                }
            </div>
            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
        <!-- HERO SHOULDER -->
        <div class="ab-services">
            <div class="row">
                @{
                    if (heroShoulder != null)
                    {
                        foreach (var item in heroShoulder.Items)
                        {
                            var itemContent = item.Content;
                            var tabName = itemContent.Value(PublishedValueFallback, "tabName")?.ToString() ?? string.Empty;
                            string imgshoulderWidth = "40";
                            string linkValue = null;
                            if (tabName == "Borrow")
                            {
                                imgshoulderWidth = "55";

                            }
                            string tabBgColor = "";
                            if (tabName == "Bank")
                            {
                                tabBgColor = "ab-bank-service";
                            }
                            else if (tabName == "Borrow")
                            {
                                tabBgColor = "ab-borrow-service";
                            }
                            else if (tabName == "Invest")
                            {
                                tabBgColor = "ab-invest-service";
                            }
                            else
                            {
                                tabBgColor = "ab-insure-service";
                            }
                            foreach (var subI in itemContent.Value<BlockListModel>(PublishedValueFallback, "items") ?? Enumerable.Empty<BlockListItem>())
                            {
                                var subItem = (StructureBeCardDescription)subI.Content;
                                <div class="col-md-3 col-6">

                                    <div class="container @tabBgColor.ToString()">
                                        @if ((subItem.FormatIcon) && (subItem.Link != null))
                                        {
                                            <img src="@subItem.BenefitCardIcon.Url(PublishedUrlProvider)" alt="@subItem.BenefitCardIcon.Value(PublishedValueFallback, "altTag")" width=@imgshoulderWidth.ToString() height="50">
                                        }
                                        else
                                        {
                                            <img src="@subItem.BenefitCardIcon.Url(PublishedUrlProvider)" alt="@subItem.BenefitCardIcon.Value(PublishedValueFallback, "altTag")" width=@imgshoulderWidth.ToString() height="50">
                                        }
                                        <p>@subItem.Value(PublishedValueFallback, "benefitCardTitle")<br />@subItem.Value(PublishedValueFallback, "benefitCardDescription")</p>

                                        <div class="secondary-btn-dark">
                                            @if (subItem.Link != null)
                                            {
                                                <a href="@subItem.Link.Url(PublishedUrlProvider)"><button>@tabName</button></a>
                                            }
                                            else
                                            {
                                                <a href="@linkValue"><button>@tabName</button></a>
                                            }
                                        </div>
                                    </div>

                                </div>
                            }
                        }
                    }
                }
            </div>
        </div>
    </div>
</div>
<!-----------Header Section Ends--------------->
<!-----------Section 1 Starts------------------->
@{
    if (heroBodySections != null)
    {
        int getLoanCount = 0;
        foreach (var getGetLoans in heroBodySections)
        {
            if (getLoanCount == 0)
            {
                Umbraco.Cms.Core.Models.Link getLoansLink = null;
                foreach (var link in (IEnumerable<Umbraco.Cms.Core.Models.Link>)getGetLoans.Value(PublishedValueFallback, "sectionLink"))
                {
                    getLoansLink = link;
                    break;
                }
                <div class="ab-below-fold-section special-margin" data-aos="fade-up" data-aos-easing="linear" data-aos-duration="1500" style="">
                    <div class="container row section-a h-100">
                        <div class="col-md-6  section-artwork">
                            @if (getGetLoans.Value(PublishedValueFallback, "ebankItimage") != null)
                            {
                                var sectionImageUrl = ((ContentModels.Image)((IPublishedContent)getGetLoans.Value(PublishedValueFallback, "ebankItimage"))).Url(PublishedUrlProvider);
                                <img src="@sectionImageUrl" width="90%" alt="@getGetLoans.Value(PublishedValueFallback, "altTag")" width="90%" height="100%">
                            }else{
                                <img src="" width="90%" alt="@getGetLoans.Value(PublishedValueFallback, "altTag")">
                            }
                        </div>
                        <div class="col-md-6 section-content my-auto">
                            <h2 class="ab-section-title">@getGetLoans.Value(PublishedValueFallback, "ebankITTitle")</h2>
                            <h3 class="ab-section-discription">@getGetLoans.Value(PublishedValueFallback, "ebankITContent")</h3>
                            <p>@getGetLoans.Value(PublishedValueFallback, "sectionHeroBodyDescription")</p>
                            <div class="primary-btn instant-loan">

                                @if (getLoansLink != null)
                                {

                                    if (@getLoansLink.Name.ToString() != "" || @getLoansLink.Url.ToString() != "")
                                    {
                                        <a href="@getLoansLink.Url"><button>@getLoansLink.Name</button></a>
                                    }
                                }
                            </div>
                        </div>
                    </div>
                </div>
            }
            getLoanCount++;
        }
    }
}
<!-----------Section 1 Ends--------------------->
<!-----------Section 2 Starts------------------->
<div class="flex-column-reverse" data-aos="fade-up" data-aos-easing="linear" data-aos-duration="1500" style="background: 0% 0% no-repeat padding-box padding-box rgb(0, 29, 66);">
    <div class="container row section-a h-100 flex-row-reverse">
        @{
            int myWorldCount = 0;
            foreach (var getMyWorldBank in heroBodySections)
            {
                if (myWorldCount == 1)
                {
                    Umbraco.Cms.Core.Models.Link getWorldLink = null;
                    foreach (var link in (IEnumerable<Umbraco.Cms.Core.Models.Link>)getMyWorldBank.Value(PublishedValueFallback, "sectionLink"))
                    {
                        getWorldLink = link;
                        break;
                    }
                    <div class="col-md-6 section-artwork my-auto">
                        @if (getMyWorldBank.Value(PublishedValueFallback, "ebankItimage") != null)
                        {
                            var bankImageUrl = ((ContentModels.Image)((IPublishedContent)getMyWorldBank.Value(PublishedValueFallback, "ebankITImage"))).Url(PublishedUrlProvider);
                            <img src="@bankImageUrl" width="60%" alt="MyWorld Bank">
                        }else{
                            <img src="" alt="">
                        }
                    </div>
                    <div class="col-md-6 section-content">
                        <h2 class="ab-section-title-myworld">@getMyWorldBank.Value(PublishedValueFallback, "ebankITTitle")</h2>
                        <h3 class="ab-section-discription-myworld">@getMyWorldBank.Value(PublishedValueFallback, "ebankITContent")</h3>
                        <p class="ab-body-desc-myworld">@Html.StripHtml(@getMyWorldBank.Value(PublishedValueFallback, "sectionHeroBodyDescription").ToString(), new[] { "p" })</p>
                        <div class="primary-btn myworld-button-padding">
                            @if (getWorldLink != null)
                            {
                                <a href="@getWorldLink.Url"><button>@getWorldLink.Name</button></a>
                            }
                        </div>
                    </div>
                }
                myWorldCount++;
            }
        }
    </div>
</div>
<!-----------Section 2 Ends--------------------->
<!-----------Section 3 Starts------------------->
@{
    int supportCount = 0;

    foreach (var getSupportData in heroBodySections)
    {
        if (supportCount == 2)
        {
            <div class="ab-below-fold-section" data-aos="fade-up" data-aos-easing="linear" data-aos-duration="1500" style="url()">
                <div class="container row section-a custom-margin h-100">

                    <div class="col-md-6  section-artwork my-auto">
                        @if (getSupportData.Value(PublishedValueFallback, "ebankItimage") != null)
                        {
                            var supportImageUrl = ((ContentModels.Image)((IPublishedContent)getSupportData.Value(PublishedValueFallback, "ebankITImage"))).Url(PublishedUrlProvider);
                            <img src="@supportImageUrl" width="90%" height="100%" alt="@getSupportData.Value(PublishedValueFallback, "ebankITTitle")">
                        }
                        else
                        {
                            <img src="" width="90%" alt="">
                        }
                    </div>
                    <div class="col-md-6 section-content my-auto">
                        <h2 class="ab-section-title">@getSupportData.Value(PublishedValueFallback, "ebankITTitle")</h2>
                        <h3 class="ab-section-discription">@getSupportData.Value(PublishedValueFallback, "ebankITContent")</h3>
                        <p>@getSupportData.Value(PublishedValueFallback, "sectionHeroBodyDescription")</p>
                        <p><a href="#">LIVE CHAT</a> | <a href="#">CALL ME BACK</a> | <a href="#">ONLINE INSTANT APPLICATION</a> </p>

                    </div>
                </div>
            </div>
        }
        supportCount++;
    }
}
<!-----------Section 3 Ends--------------------->
<!-----------Section 4 Starts------------------->
<div data-aos="fade-up" data-aos-easing="linear" data-aos-duration="1500" style="background: 0% 0% no-repeat padding-box padding-box rgb(0, 29, 66);">
    <div class="container text-center section-blog">
        @{
            int blogSectionCount = 0;
            Umbraco.Cms.Core.Models.Link blogPageLink = null;
            foreach (var getblogSection in heroBodySections)
            {
                if (blogSectionCount == 3)
                {
                  foreach (var link in (IEnumerable<Umbraco.Cms.Core.Models.Link>)getblogSection.Value(PublishedValueFallback, "sectionLink"))
                    {
                        blogPageLink = link;
                        break;
                    }
                    <h1 class="blog-title">@getblogSection.Value(PublishedValueFallback, "ebankITTitle")</h1>
                    <h2 class="blog-discription">@getblogSection.Value(PublishedValueFallback, "ebankITContent")</h2>
                }
                blogSectionCount++;
            }
        }
        <div class="row">
            @{
                var blogParent = Umbraco.Content(2855);


                if (blogParent != null)
                {
                    int myCounter = 0;
                    int blogcount = 0;
                    foreach (var blog in blogParent.Children(VariationContextAccessor).Take(3))
                    {
                        if (blog.Children(VariationContextAccessor).Any())
                        {

                            foreach (var blogElement in blog.Children(VariationContextAccessor).LastOrDefault().Children(VariationContextAccessor))
                            {
                                if (blogElement.Value(PublishedValueFallback, "title") != null)
                                {

                                    string blogBoxClass = "";
                                    if (blogcount == 0)
                                    {
                                        blogBoxClass = "blog-first-box";

                                    }
                                    else if (blogcount == 1)
                                    {
                                        blogBoxClass = "blog-second-box text-center";
                                    }
                                    else
                                    {
                                        blogBoxClass = "blog-third-box";
                                    }

                                    var blogHeader = blog.Children(VariationContextAccessor).FirstOrDefault().Children(VariationContextAccessor).Where(c => c is HeroHeaderBlogDetail).FirstOrDefault();
                                    var ImageUrl = ((IPublishedContent)blogHeader.Value(PublishedValueFallback, "image")).Url(PublishedUrlProvider)?.ToString() ?? "";
                                    <div class="col-md-4 col-12">
                                        <div class="container @blogBoxClass.ToString()"
                                        style="background-image:linear-gradient(rgba(128,128,128,0),rgba(0,0,0,1)),url(@ImageUrl);background-position:center;background-repeat:no-repeat; background-size: cover;border-radius: 10px;Width: 95%; Margin:auto">
                                            <p class="ab-thumbnail-card">@blogElement.Value(PublishedValueFallback, "title")</p>
                                            <p class="ab-thumbnail-desc limited-text">@htmlStringUtilities.Truncate(@blogElement.Value(PublishedValueFallback, "introductionDescription").ToString(), 85, true, false)</p>
                                            <div class="ab-blog-button"><a href="@blog.Url(PublishedUrlProvider)"><button>Read More</button></a></div>
                                            <p class="ab-thumbnail-desc" style="padding-top:10px; padding-bottom:10px">Published: @(blogElement.Value<DateTime>(PublishedValueFallback, "blogDate").ToShortDateString())</p>
                                        </div>
                                    </div>
                                    myCounter++;
                                    blogcount++;
                                }

                            }

                        }

                    }
                }
            }
        </div>
        <div class="row">
            <div class="col-12">
                <div class="text-center ab-viewmore-button">
                    @if (blogPageLink != null)
                    {
                        <a href="@blogPageLink.Url"><button>View more</button></a>
                    }
                    else
                    {
                        <a href="#"><button>View more</button></a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<!-------- We can help tab ------------>
@*@{
        int helpCount = 0;
        foreach (var getHelpData in heroBodySections)
        {
            if (helpCount == 3)
            {
                var helpBgImageUrl = ((ContentModels.Image)((IPublishedContent)getHelpData.Value("sectionHeroBodyBackground"))).Url;
                var helpBgImageUrl1 = ((ContentModels.Image)((IPublishedContent)getHelpData.Value("ebankITImage"))).Url;
                <div class="ab-light-blue-section" style="margin-top: -100px;background-image:url('@helpBgImageUrl');" data-aos="fade-up" data-aos-easing="linear" data-aos-duration="1500">
                    <div class="container row section-a text-center">
                        <div class="col-md-12 section-content">
                            <h2 class="ab-section-white-title">@getHelpData.Value("ebankITTitle")</h2>
                            <h3 class="ab-section-discription">@getHelpData.Value("ebankITContent")</h3>
                            <form id="frmHelp" method="post" class="text-center">
                                <div class="form-group row">
                                    <label class="col-sm-5 col-form-label ab-form-label">Name and Surname</label>
                                    <div class="col-sm-7">
                                        <input class="form-control form-control-sm ab-input-field" name="nameSurname" type="text" placeholder="eg. Luke Smith" required>
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <label class="col-sm-5 col-form-label ab-form-label">Contact Number</label>
                                    <div class="col-sm-7">
                                        <input class="form-control form-control-sm ab-input-field" name="contactNumber" type="text" placeholder="eg. 07123456789" required>
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <label class="col-sm-5 col-form-label ab-form-label">Email Address</label>
                                    <div class="col-sm-7">
                                        <input class="form-control form-control-sm ab-input-field" name="emailAdd" type="text" placeholder="eg. l.smith@email.co.za" required>
                                    </div>
                                </div>
                                <div class="form-group row">
                                    <label class="col-sm-5 col-form-label special-color ab-form-label">What can we help you with?</label>
                                    <div class="col-sm-7">
                                        <div class="dropdown show dropdown-special">
                                            <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-expanded="false">
                                                <div class=" ab-select-field" >I want to..</div>
                                            </a>
                                            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                                                <a class="dropdown-item" href="#">I want to Open MyWorld Bank Account</a>
                                                <a class="dropdown-item" href="#">I want to Apply for a loan</a>
                                                <a class="dropdown-item" href="#">I want to Invest my money</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="form-group row">
                                    <label class="col-sm-5 col-form-label ab-form-label"></label>
                                    <div class=" col-sm-7">
                                        <div class="primary-btn" style="float: left; padding-left: 12px;">
                                            <button style="width:300px;">Please Call me back</button>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            }
            helpCount++;
        }
    }*@
<!-----------Section 4 Ends--------------------->
<!---------Testimonial part start--------------->
<div class="container text-center">
    <h1 class="ab-test-title">Testimonials</h1>
    <h2 class="test-header">@testimonialsNew.Value(PublishedValueFallback, "testimonialHeading")</h2>
    <div id="carouselExample" class="carousel slide ab-test-adjust" data-ride="carousel">
        @{
            if (testimonialsNew != null)
            {
                <div class="carousel-inner">
                    @{
                        int newCounter = 0;
                        foreach (var testimonyItem in testimonialsNew.TestimonialItems)
                        {
                            var testimonyContent = testimonyItem.Content;
                            var Avatar = ((ContentModels.Image)((IPublishedContent)testimonyContent.Value(PublishedValueFallback, "testimonialAvatar"))).Url(PublishedUrlProvider);
                            <div class="carousel-item @((newCounter == 0) ? "active" : "")">
                                <div class="row h-100">
                                    <div class="col col-sm-3 col-sm-offset-4 overlay-img my-auto align-items-center">
                                        <img class="overlay-img" src="@Avatar" loading="lazy" width="90%" height="100%">
                                    </div>
                                    <div class="col col-sm-7 test-desc w-100">
                                        @testimonyContent.Value(PublishedValueFallback, "testimonialStory")
                                        <p class="ab-test-border"></p>
                                        <p class="ab-test-footer">@testimonyContent.Value(PublishedValueFallback, "testimonialName")</p>
                                    </div>
                                </div>
                            </div>
                            newCounter++;
                        }
                    }
                </div>
            }
            <a class="carousel-control-prev" href="#carouselExample" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExample" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        }
    </div>
</div>
<!----------Testimonial part end---------->
