@*
    Placeholder - the real v8 markup for this partial was never present in
    this checkout. Referenced unconditionally from homeV2.cshtml (the
    homepage slider/carousel section), so a missing file was crashing that
    page. Replace with the real content once it's sourced.
*@
