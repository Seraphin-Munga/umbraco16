@*
    Placeholder - the real v8 navigation markup for this partial was never
    present in this checkout (Platform/Web/Views/Partials/Navigation was
    already empty in the source). Replace this file with the actual content
    once it's available; every layout (master.cshtml, masterPublic.cshtml,
    MasterNew.cshtml, MasterHeaderOnly.cshtml, ebankITMasterPublic.cshtml)
    renders it unconditionally, so an empty/missing file was crashing every
    page.
*@
<nav class="navbar"></nav>
